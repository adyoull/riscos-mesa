diff -ruN mesa-a/include/c11/threads_posix.h mesa-b/include/c11/threads_posix.h
--- a/include/c11/threads_posix.h	2026-09-24 22:54:50.718555224 +0000
+++ b/include/c11/threads_posix.h	2026-09-24 22:54:50.735591638 +0000
@@ -43,7 +43,7 @@
     Use pthread_mutex_timedlock() for `mtx_timedlock()'
     Otherwise use mtx_trylock() + *busy loop* emulation.
 */
-#if !defined(__CYGWIN__) && !defined(__APPLE__) && !defined(__NetBSD__)
+#if !defined(__CYGWIN__) && !defined(__APPLE__) && !defined(__NetBSD__) && !defined(__riscos__)
 #define EMULATED_THREADS_USE_NATIVE_TIMEDLOCK
 #endif
 
diff -ruN mesa-a/meson.build mesa-b/meson.build
--- a/meson.build	2026-09-24 21:26:06.165897522 +0000
+++ b/meson.build	2026-09-25 04:45:44.372005850 +0000
@@ -436,7 +436,7 @@
 
 # Android uses emutls for versions <= P/28. For USE_ELF_TLS we need ELF TLS.
 use_elf_tls = false
-if not ['windows', 'freebsd', 'openbsd'].contains(host_machine.system()) and (not with_platform_android or get_option('platform-sdk-version') >= 29)
+if not ['windows', 'freebsd', 'openbsd', 'riscos'].contains(host_machine.system()) and (not with_platform_android or get_option('platform-sdk-version') >= 29)
   pre_args += '-DUSE_ELF_TLS'
   use_elf_tls = true
 endif
@@ -943,7 +943,7 @@
 endif
 
 # TODO: this is very incomplete
-if ['linux', 'cygwin', 'gnu', 'freebsd', 'gnu/kfreebsd'].contains(host_machine.system())
+if ['linux', 'cygwin', 'gnu', 'freebsd', 'gnu/kfreebsd', 'riscos'].contains(host_machine.system())
   pre_args += '-D_GNU_SOURCE'
 elif host_machine.system() == 'sunos'
   pre_args += '-D__EXTENSIONS__'
@@ -1308,7 +1308,7 @@
 # check for dl support
 dep_dl = null_dep
 if not cc.has_function('dlopen')
-  dep_dl = cc.find_library('dl', required : host_machine.system() != 'windows')
+  dep_dl = cc.find_library('dl', required : not ['windows', 'riscos'].contains(host_machine.system()))
 endif
 if cc.has_function('dladdr', dependencies : dep_dl)
   # This is really only required for megadrivers
@@ -1351,7 +1351,12 @@
   dep_zstd = null_dep
 endif
 
-dep_thread = dependency('threads')
+if host_machine.system() == 'riscos'
+  # UnixLib has pthreads built in and GCCSDK's GCC rejects -pthread
+  dep_thread = declare_dependency()
+else
+  dep_thread = dependency('threads')
+endif
 if dep_thread.found() and host_machine.system() != 'windows'
   pre_args += '-DHAVE_PTHREAD'
   if host_machine.system() != 'netbsd' and cc.has_function(
diff -ruN mesa-a/src/mesa/drivers/osmesa/meson.build mesa-b/src/mesa/drivers/osmesa/meson.build
--- a/src/mesa/drivers/osmesa/meson.build	2026-09-24 21:26:06.189817578 +0000
+++ b/src/mesa/drivers/osmesa/meson.build	2026-09-24 21:26:15.732547137 +0000
@@ -24,7 +24,7 @@
   osmesa_link_with += libglapi
 endif
 
-libosmesa = shared_library(
+libosmesa = library(
   osmesa_lib_name,
   ['osmesa.c', glapitable_h, glapitemp_h],
   gnu_symbol_visibility : 'hidden',
diff -ruN mesa-a/src/util/detect_os.h mesa-b/src/util/detect_os.h
--- a/src/util/detect_os.h	2026-09-24 21:26:06.174756647 +0000
+++ b/src/util/detect_os.h	2026-09-24 21:26:15.731983112 +0000
@@ -52,6 +52,12 @@
 #define DETECT_OS_UNIX 1
 #endif
 
+/* RISC OS via GCCSDK/UnixLib: a POSIX-ish userland without /proc */
+#if defined(__riscos__)
+#define DETECT_OS_RISCOS 1
+#define DETECT_OS_UNIX 1
+#endif
+
 #if defined(__GNU__)
 #define DETECT_OS_HURD 1
 #define DETECT_OS_UNIX 1
@@ -124,6 +130,9 @@
 #ifndef DETECT_OS_UNIX
 #define DETECT_OS_UNIX 0
 #endif
+#ifndef DETECT_OS_RISCOS
+#define DETECT_OS_RISCOS 0
+#endif
 #ifndef DETECT_OS_WINDOWS
 #define DETECT_OS_WINDOWS 0
 #endif
diff -ruN mesa-a/src/util/memstream.c mesa-b/src/util/memstream.c
--- a/src/util/memstream.c	2026-09-25 04:47:58.934710935 +0000
+++ b/src/util/memstream.c	2026-09-25 04:47:58.948507304 +0000
@@ -51,7 +51,8 @@
    }
 
    return success;
-#elif defined(__APPLE__)
+#elif defined(__APPLE__) || defined(__riscos__)
+   /* No open_memstream (UnixLib): callers fall back as on macOS */
    return false;
 #else
    FILE *const f = open_memstream(bufp, sizep);
diff -ruN mesa-a/src/util/os_misc.c mesa-b/src/util/os_misc.c
--- a/src/util/os_misc.c	2026-09-24 21:26:06.179256693 +0000
+++ b/src/util/os_misc.c	2026-09-24 21:26:15.732203104 +0000
@@ -65,6 +65,8 @@
 #  include <sys/sysctl.h>
 #elif DETECT_OS_HAIKU
 #  include <kernel/OS.h>
+#elif DETECT_OS_RISCOS
+#  include <unistd.h>
 #elif DETECT_OS_WINDOWS
 #  include <windows.h>
 #else
@@ -184,6 +186,10 @@
    ret = GlobalMemoryStatusEx(&status);
    *size = status.ullTotalPhys;
    return (ret == TRUE);
+#elif DETECT_OS_RISCOS
+   /* No portable query; callers treat false as "unknown". */
+   (void)size;
+   return false;
 #else
 #error unexpected platform in os_sysinfo.c
    return false;
diff -ruN mesa-a/src/util/u_endian.h mesa-b/src/util/u_endian.h
--- a/src/util/u_endian.h	2026-09-24 21:26:35.673839223 +0000
+++ b/src/util/u_endian.h	2026-09-24 21:26:35.705781259 +0000
@@ -89,6 +89,12 @@
 
 #endif
 
+/* RISC OS: ARM little-endian; don't depend on UnixLib's <endian.h> macros */
+#if defined(__riscos__) && !defined(UTIL_ARCH_LITTLE_ENDIAN)
+#define UTIL_ARCH_LITTLE_ENDIAN 1
+#define UTIL_ARCH_BIG_ENDIAN 0
+#endif
+
 #if !defined(UTIL_ARCH_LITTLE_ENDIAN) || !defined(UTIL_ARCH_BIG_ENDIAN)
 # error "UTIL_ARCH_LITTLE_ENDIAN and/or UTIL_ARCH_BIG_ENDIAN were unset."
 #elif UTIL_ARCH_LITTLE_ENDIAN == UTIL_ARCH_BIG_ENDIAN
diff -ruN mesa-a/src/util/u_thread.h mesa-b/src/util/u_thread.h
--- a/src/util/u_thread.h	2026-09-24 21:26:35.682641954 +0000
+++ b/src/util/u_thread.h	2026-09-24 22:59:05.876152625 +0000
@@ -113,6 +113,8 @@
    pthread_setname_np(name);
 #elif DETECT_OS_HAIKU
    rename_thread(find_thread(NULL), name);
+#elif DETECT_OS_RISCOS
+   /* UnixLib has no thread names */
 #else
 #warning Not sure how to call pthread_setname_np
 #endif
@@ -208,7 +210,7 @@
 static inline int64_t
 util_thread_get_time_nano(thrd_t thread)
 {
-#if defined(HAVE_PTHREAD) && !defined(__APPLE__) && !defined(__HAIKU__)
+#if defined(HAVE_PTHREAD) && !defined(__APPLE__) && !defined(__HAIKU__) && !defined(__riscos__)
    struct timespec ts;
    clockid_t cid;
 
@@ -248,7 +250,8 @@
  * util_barrier
  */
 
-#if defined(HAVE_PTHREAD) && !defined(__APPLE__)
+/* UnixLib has no pthread barriers: use the mutex+condvar fallback */
+#if defined(HAVE_PTHREAD) && !defined(__APPLE__) && !defined(__riscos__)
 
 typedef pthread_barrier_t util_barrier;
 
diff -ru a/include/GL/osmesa.h b/include/GL/osmesa.h
--- a/include/GL/osmesa.h	2026-09-25 17:35:55.055684799 +0100
+++ b/include/GL/osmesa.h	2026-09-25 17:35:55.070548942 +0100
@@ -106,6 +106,10 @@
 #define OSMESA_COMPAT_PROFILE        0x35
 #define OSMESA_CONTEXT_MAJOR_VERSION 0x36
 #define OSMESA_CONTEXT_MINOR_VERSION 0x37
+/* riscos-mesa: OpenGL ES contexts (values of OSMESA_PROFILE). ES 1.1
+   (common profile) and ES 2.0 are what classic swrast supports. */
+#define OSMESA_ES1_PROFILE           0x1001
+#define OSMESA_ES2_PROFILE           0x1002
 
 
 typedef struct osmesa_context *OSMesaContext;
@@ -150,7 +154,8 @@
  * OSMESA_DEPTH_BITS             0*, 16, 24, 32
  * OSMESA_STENCIL_BITS           0*, 8
  * OSMESA_ACCUM_BITS             0*, 16
- * OSMESA_PROFILE                OSMESA_COMPAT_PROFILE*, OSMESA_CORE_PROFILE
+ * OSMESA_PROFILE                OSMESA_COMPAT_PROFILE*, OSMESA_CORE_PROFILE,
+ *                               OSMESA_ES1_PROFILE, OSMESA_ES2_PROFILE
  * OSMESA_CONTEXT_MAJOR_VERSION  1*, 2, 3
  * OSMESA_CONTEXT_MINOR_VERSION  0+
  *
diff -ru a/src/mesa/drivers/osmesa/osmesa.c b/src/mesa/drivers/osmesa/osmesa.c
--- a/src/mesa/drivers/osmesa/osmesa.c	2026-09-25 17:35:55.053585113 +0100
+++ b/src/mesa/drivers/osmesa/osmesa.c	2026-09-25 17:35:55.070366879 +0100
@@ -727,6 +727,10 @@
             api_profile = API_OPENGL_COMPAT;
          else if (profile == OSMESA_CORE_PROFILE)
             api_profile = API_OPENGL_CORE;
+         else if (profile == OSMESA_ES1_PROFILE)    /* riscos-mesa */
+            api_profile = API_OPENGLES;
+         else if (profile == OSMESA_ES2_PROFILE)    /* riscos-mesa */
+            api_profile = API_OPENGLES2;
          else
             return NULL;
          break;
diff -ru a/src/compiler/glsl/ast_to_hir.cpp b/src/compiler/glsl/ast_to_hir.cpp
--- a/src/compiler/glsl/ast_to_hir.cpp	2026-09-25 18:44:44.195376859 +0100
+++ b/src/compiler/glsl/ast_to_hir.cpp	2026-09-25 18:44:44.210003836 +0100
@@ -2664,6 +2664,18 @@
 
       precision =
          state->symbols->get_default_precision_qualifier(type_name);
+      if (precision == ast_precision_none &&
+          state->stage == MESA_SHADER_FRAGMENT &&
+          strcmp(type_name, "float") == 0) {
+         /* riscos-mesa: the Raspberry Pi's VideoCore compiler accepted
+          * fragment shaders with no default float precision, and programs
+          * written for it rely on that. Precision is only a hint here:
+          * take mediump and warn instead of failing. */
+         _mesa_glsl_warning(loc, state,
+                            "No precision specified in this scope for type `%s', "
+                            "using mediump", type->name);
+         precision = ast_precision_medium;
+      }
       if (precision == ast_precision_none) {
          _mesa_glsl_error(loc, state,
                           "No precision specified in this scope for type `%s'",
