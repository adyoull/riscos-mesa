riscos-mesa 20.3.5-3 test programs (static ELF, GCCSDK GCC 10.2.0)
===========================================================

Needs: RISC OS 5, ARMv7 with VFP (Pi 2/3/4), SharedUnixLibrary and
ARMEABISupport loaded (same as the OpenTTD build). Best in a 16M-colour
screen mode. Run from a TaskWindow or F12 command line, in this directory.
The zip carries RISC OS filetypes: the programs unzip as ELF (&E1F) with
SparkFS or the RISC OS unzip.

Run in this order and note the output:

1. orient
   Checks row order and pixel byte order. Should print row0 = ff 00 00 ..,
   row15 = 00 00 ff ..  Exit code 0 means pass.

2. prof
   Lists which GL versions are granted. Expected: compat 2.1 works,
   3.x and core are NULL.

3. osmesatest 320 240 100
   Prints GL strings, fixed-function fps and GLSL fps, then saves the last
   frame as a Sprite file "gltest" in the current directory (open in Paint:
   a shaded cube on dark red). Try 640 480 50 too.

4. glutest
   GLU check. Prints GLU 1.3 and "centre px=ff".

5. sdlgltest 640 480
   SDL2 OpenGL with a spinning lit cube. Run from the desktop it opens in a
   normal desktop window; the title bar shows fps and each frame's render
   (Mesa) and present (plotting) time. Keys (click in the window first):
     F      toggle full screen and back
     Space  toggle vsync (in a window this yields to other tasks)
     Wheel  zoom in and out
     Escape quit (closing the window also works)
   Options: -f start full screen, -t N quit after N seconds,
   -r N cap at N fps using SDL_Delay, -w only redraw when something happens
   (idle = no CPU). It prints a summary (average fps, render/present ms)
   when it quits.

6. glbench [width height [seconds [scene]]] [-o file]
   Software GL benchmark, no desktop involved: clear, lit cube, full-screen
   texture, blending, 12288 triangles and GLSL, in ms per frame. Scene
   names: clear cube tex blend tris glsl. Use -o file to save the results
   (rather than "> file"). Reports the address and RISC OS error if
   anything crashes.

7. egltest  (the new EGL library)
   The egl-* Obey files run each case with a double-click and save the
   results next to them (eglcheck, eglwindow, eglfull, ...).
   a) egltest -o eglcheck
      No desktop needed: EGL strings, the config list, a pbuffer and a
      pixmap (GL drawing straight into a sprite), error cases. Ends with
      PASS or FAIL. Also saves the Sprite file "eglpixmap": open it in
      Paint (a lit cube on dark red with 4 green pixels top left).
   b) egltest -w  (from the desktop, e.g. double-click or TaskWindow)
      Desktop window with a spinning cube, fps and render/present ms in
      the title. The scroll wheel zooms. Resize it, drag other windows over
      it, and close it to finish. In a window there's no vsync (waiting
      would stop the desktop), so some tearing is expected.
      Add -r for a second, small GL view (a cube on dark red, spinning the
      other way) at a fixed work area position: this window has scroll
      bars, and the small view should scroll with the window's contents. Add -t N to stop after N seconds and -o file to
      save the summary.
   c) Full screen over the current mode for 5 s (-t N to change). The
      desktop is frozen while it runs and redrawn afterwards.
      egl-full          sprite plotted after the vsync wait (default)
      egl-full-novsync  the same without waiting (maximum speed)
      egl-direct        draws straight into the visible screen (tears)
      egl-banks         EXPERIMENTAL: 3 screen banks, switched at vsync
      egl-banks-fv      the same, switching before the vsync wait
      egl-tear          a white bar sweeping across black, plotted as
                        egl-full: tearing shows as the bar broken sideways
      egl-tear-banks    the same bar with 3 screen banks
      For each: does it tear, and does the tear stay at one height or move?

   d) egl-window2b: as egl-window2, but the small view has its own GL
      context. Both window2 tests now save a line "second surface check"
      in their results and a Sprite file "eglfx" (the small view's
      memory): please send those.

   e) egl-fx-first, egl-fx-big, egl-fx-corner: the small view created
      first, 400x300, and at the work area's corner. All should show the
      small spinning cube. (On the Pi 4 small sprites are cached between
      plots, so the library pads small surfaces' sprites to 1 MB.)

   f) egl-damage: the extensions in 20.3.5-4. After the first frame only
      the middle half of the window is redrawn and shown (buffer age and
      swap with damage). The middle's background slowly changes colour
      while the edges keep the first frame's blue. Compare the present
      time in the title with egl-window. egl-check now also checks the
      new extensions (sync objects, surfaceless contexts, surface
      locking, the debug callback).

Please report: the fps figures, anything that crashes (with the error
text), the fps in the window title, whether the scroll wheel zooms (in a window
and in full screen), whether the cube redraws after you drag
another window over it, and whether F (full screen and back) leaves the
desktop redrawn properly.
